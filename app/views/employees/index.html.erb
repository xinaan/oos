<div class="col-md-10">
<h2 class="page-header">Employees <%= link_to 'New Employee', new_employee_path, class:"btn btn-success pull-right" %></h2>
<table id="example" class="table table-striped table-bordered table-hover dataTable no-footer" >
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Username</th>
                <th>DOB</th>
                <th>Email</th>
				<th>Code</th>
				<th>Verified</th>
				<th>Actions</th>
            </tr>
        </thead>
 
        <tbody>
		<% @employees.each do |employee| %>
            <tr>
                <td><%= employee.first_name %></td>
                <td><%= employee.last_name %></td>
                <td><%= employee.user.username %></td>
                <td><%= employee.dob %></td>
                <td><%= employee.user.email %></td>
				<td><%= employee.user.activation_code %></td>
				<td><%= employee.user.is_verified %></td>
				<td class="text-center"><a href="<%= edit_employee_path(employee) %>"> <i class="fa fa-pencil fa-lg"></i></a>
					<a data-confirm="Are you sure?" rel="nofollow" data-method="delete" href="<%= employee_path(employee)%>"><i class="fa fa fa-trash-o fa-lg text-danger"></i></a>
				</td>
            </tr>
		<% end %>
		</tbody>
</table>
</div>




	<script>
$(document).ready(function() {
    $('#example').dataTable();
} );
</script>
